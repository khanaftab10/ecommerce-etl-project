CREATE TABLE sales (
    order_id INT,
    order_date DATE,
    customer_name VARCHAR(100),
    city VARCHAR(50),
    product VARCHAR(100),
    quantity INT,
    price NUMERIC(10,2),
    total_sales NUMERIC(10,2)
);